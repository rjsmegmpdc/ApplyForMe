# Copy to .dev.vars for local `next dev` / `wrangler dev`. Never commit .dev.vars.
ANTHROPIC_API_KEY=
TOKENS_ENC_KEY=
ACTION_LINK_SECRET=
# Leave ACCESS_* unset locally: identity falls back to user 1 (dev-mode escape hatch).
# ACCESS_TEAM_DOMAIN=
# ACCESS_APP_AUD=
