/** Placeholder — replaced by the pipeline task. */
export async function handleScheduledSweep(
  _event: ScheduledController,
  _env: CloudflareEnv,
  _ctx: ExecutionContext
): Promise<void> {
  console.log('scheduled sweep; pipeline not wired yet');
}
